---
name: updated-bullmq-version
description: Fixed BullMQ dependency to a real 2.x latest release.
metadata:
  type: project
---

- Updated `package.json` to set `bullmq` to `^2.4.0` (the latest 2.x release).
- No further changes to other packages were necessary.
