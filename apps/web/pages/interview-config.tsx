import type { NextPage } from 'next';
import Head from 'next/head';

const InterviewConfig: NextPage = () => (
  <>
    <Head>
      <title>Interview Configuration - Interview Coach</title>
    </Head>
    <main className="flex min-h-screen flex-col items-center justify-center p-4"
    >
      <h1 className="text-2xl font-bold">Interview Configuration</h1>
      {/* Config form placeholder */}
    </main>
  </>
);

export default InterviewConfig;
