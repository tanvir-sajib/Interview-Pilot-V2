# Interview Coach AI Service

FastAPI-based microservice responsible for AI tasks such as transcription, LLM evaluation, and delivery analysis.

## Running

```bash
cd ai-service
pip install -r requirements.txt
uvicorn main:app --reload --port 8000
```
