# Dockerfile for the API service
FROM node:20-alpine
WORKDIR /app
# Copy only package info first for caching
COPY apps/api/package*.json ./
RUN npm install
COPY apps/api/ ./
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
